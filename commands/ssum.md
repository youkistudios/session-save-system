---
description: Alias for /session-summary — close out with readable + technical reports
---
Invoke the `session-summary` skill now (the `/ssum` close-out) for the current session, following the rulebook GUIDE.md in the save-system home folder ($SAVE_SYSTEM_HOME, default ~/Desktop/session-logs/).
