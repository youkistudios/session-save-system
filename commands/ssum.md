---
description: Alias for /session-summary — close out with readable + technical reports
---
Invoke the `session-summary` skill now (the `/ssum` close-out) for the current session, following the shared rulebook GUIDE.md in the home resolved by the installed Session Save kernel.
